{
  "name": "msp-trees-scripts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Data pipeline: shapefile → GeoJSON → (later) PMTiles + D1",
  "scripts": {
    "extract": "bun run extract.mjs",
    "deploy:cf": "bun run deploy-cloudflare.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "shapefile": "^0.6.6"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
